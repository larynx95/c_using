/* Data Structures Using C, 2nd, Reema Thareja
Chapter 03. Arrays

Review Questions 13.
Consider a two-dimensional array arr[10][10]
which has base address = 1000 and the number of
bytes per element of the array = 2. Now, compute
the address of the element arr[8][5] assuming
that the elements are stored in column major order.
*/

#include <stdio.h>
#include <stdlib.h>

int main() {
  //
  return 0;
}
